# SongsRepository
This repository host an R package I created that contains a data set with 2000 tracks from a kaggle sourced frame.
The original sourced data was further cleaned by me for easier usage.
